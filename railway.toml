[build]
builder       = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand         = "python agent.py start"
restartPolicyType    = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath      = ""   # agent has no HTTP port — Railway monitors the process
